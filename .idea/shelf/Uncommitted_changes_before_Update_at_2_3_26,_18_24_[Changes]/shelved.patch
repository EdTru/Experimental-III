Index: Fractales/Medida de area por imagen.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import cv2\nimport numpy as np\n\nimg1 = cv2.imread(\"shapes.png\")\nimg = cv2.cvtColor(img1, cv2.COLOR_BGR2GRAY)\nret,thresh = cv2.threshold(img,10,255,0)\ncontours,hierarchy = cv2.findContours(thresh, 1, 2)\nprint(\"Number of contours in image:\",len(contours))\n\nfor i, cnt in enumerate(contours):\n   M = cv2.moments(cnt)\n   if M['m00'] != 0.0:\n      x1 = int(M['m10']/M['m00'])\n      y1 = int(M['m01']/M['m00'])\n   area = cv2.contourArea(cnt)\n   perimeter = cv2.arcLength(cnt, True)\n   perimeter = round(perimeter, 4)\n   print(f'Area of contour {i+1}:', area)\n   print(f'Perimeter of contour {i+1}:', perimeter)\n   img1 = cv2.drawContours(img1, [cnt], -1, (0,255,255), 3)\n   cv2.putText(img1, f'Area :{area}', (x1, y1), cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)\n   cv2.putText(img1, f'Perimeter :{perimeter}', (x1, y1+20), cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)\n\ncv2.imshow(\"Image\", img1)\ncv2.waitKey(0)\ncv2.destroyAllWindows()
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Fractales/Medida de area por imagen.py b/Fractales/Medida de area por imagen.py
--- a/Fractales/Medida de area por imagen.py	(revision 2d58c10183e25706fe59e7c89f6c49323732f008)
+++ b/Fractales/Medida de area por imagen.py	(date 1772471134117)
@@ -1,10 +1,10 @@
 import cv2
 import numpy as np
 
-img1 = cv2.imread("shapes.png")
+img1 = cv2.imread("P5.jpeg")
 img = cv2.cvtColor(img1, cv2.COLOR_BGR2GRAY)
-ret,thresh = cv2.threshold(img,10,255,0)
-contours,hierarchy = cv2.findContours(thresh, 1, 2)
+ret,thresh = cv2.threshold(img,80,400,0)
+contours,hierarchy = cv2.findContours(thresh, 2, 2)
 print("Number of contours in image:",len(contours))
 
 for i, cnt in enumerate(contours):
Index: Fractales/Calculos.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Fractales/Calculos.py b/Fractales/Calculos.py
new file mode 100644
--- /dev/null	(date 1772471995369)
+++ b/Fractales/Calculos.py	(date 1772471995369)
@@ -0,0 +1,51 @@
+import scipy.io
+import numpy as np
+from scipy.optimize import curve_fit
+from matplotlib import pyplot as plt
+
+papel_suave_2 = np.array([[1,14.6],
+                        [2, 22.2],
+                        [4, 35.8],
+                        [8, 38.5],
+                        [16, 54.8],
+                        [32, 78.2],
+                        [64, 119]])
+
+print(papel_suave_2)
+
+t_medido = np.transpose(papel_suave_2)[0]
+y_medido = np.transpose(papel_suave_2)[1]
+
+print(t_medido, y_medido)
+
+log_t = np.copy(t_medido)
+log_y = np.copy(y_medido)
+
+for i in range(7):
+    log_t[i] = np.log(t_medido[i])
+    log_y[i] = np.log(y_medido[i])
+
+print(log_t, log_y)
+
+def test_function(x,a,b):
+    return a*x+b
+
+param, param_cov = curve_fit(test_function, log_t, log_y)
+
+fit_f = param[0]* t_medido +param[1]
+
+# Graficar la señal original y la reconstruida
+plt.subplot(2, 1, 1)
+plt.plot(t_medido, y_medido)
+plt.title('Señal original en el dominio del tiempo')
+plt.xlabel('Tiempo [s]')
+plt.ylabel('Amplitud')
+
+plt.subplot(2, 1, 2)
+plt.plot(log_t, fit_f)
+plt.title('Señal original en el dominio del tiempo')
+plt.xlabel('Tiempo [s]')
+plt.ylabel('Amplitud')
+
+plt.tight_layout()
+plt.show()
Index: Fractales/Prueba2.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Fractales/Prueba2.py b/Fractales/Prueba2.py
new file mode 100644
--- /dev/null	(date 1772471550223)
+++ b/Fractales/Prueba2.py	(date 1772471550223)
@@ -0,0 +1,8 @@
+import numpy as np
+import cv2 as cv
+from matplotlib import pyplot as plt
+
+img = cv.imread('PapelComprimido.jpeg')
+assert img is not None, "file could not be read, check with os.path.exists()"
+gray = cv.cvtColor(img,cv.COLOR_BGR2GRAY)
+ret, thresh = cv.threshold(gray,0,255,cv.THRESH_BINARY_INV+cv.THRESH_OTSU)
\ No newline at end of file
Index: Fractales/PRPR.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Fractales/PRPR.py b/Fractales/PRPR.py
new file mode 100644
--- /dev/null	(date 1772471134156)
+++ b/Fractales/PRPR.py	(date 1772471134156)
@@ -0,0 +1,37 @@
+import cv2
+import numpy as np
+AREA_BASE = 54*38 #mm²
+
+img = cv2.imread('PapelAluminio.jpeg')
+#cv2.imshow('Original', img)
+
+#ret,thresh1 = cv2.threshold(img,127,255,cv2.THRESH_BINARY)
+
+#img = thresh1
+
+
+gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
+
+canny = cv2.Canny(gray, 70, 500)
+
+contours, hierarchy = cv2.findContours(canny, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_NONE)
+print("Number of Contours = " ,len(contours))
+#cv2.imshow('Canny Edges', canny)
+
+for i, cnt in enumerate(contours):
+   M = cv2.moments(cnt)
+   if M['m00'] != 0.0:
+      x1 = int(M['m10']/M['m00'])
+      y1 = int(M['m01']/M['m00'])
+   area = cv2.contourArea(cnt)
+   perimeter = cv2.arcLength(cnt, True)
+   perimeter = round(perimeter, 4)
+   print(f'Area of contour :', area)
+   #print(f'Perimeter of contour {i+1}:', perimeter)
+   img = cv2.drawContours(img, [cnt], -1, (0,255,255), 3)
+   cv2.putText(img, f'Area :{area}', (x1, y1), cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)
+
+
+
+cv2.imshow('Contours', img)
+cv2.waitKey(0)
\ No newline at end of file
